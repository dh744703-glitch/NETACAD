#include <stdio.h>
int main(void)
{
    char dia[7][10] = {"Lunes", "Martes", "Miercoles", "Jueves", "Viernes", "Sabado", "Domingo"};
    int dia;
    scanf("%d", &dia < 7)
    {
        printf("Punter: %s\n", *(dias + dia));
        printf("Indice: %s\n", dias[dia]);
    }
    else
        puts("Error, dia no reconocido");
    return 0;
}